{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "packages/web/dist"
      }
    }
  ],
  "functions": {
    "api/sync-live-data.ts": {
      "maxDuration": 840
    },
    "api/manual-sync.ts": {
      "maxDuration": 840
    }
  },
  "crons": [
    {
      "path": "/api/sync-live-data",
      "schedule": "*/15 * * * *"
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "buildCommand": "pnpm --filter @waylight/shared build && pnpm --filter @waylight/web build",
  "installCommand": "pnpm install"
}